# thread-load-balancer
A thread-safe load balancer application


This was my live coding exam from Revolut.

The problem: Create a load balancer. 
Basic requirements: there can only be 10 server instances and the servers must be unique instances, they cannot be repeated.
